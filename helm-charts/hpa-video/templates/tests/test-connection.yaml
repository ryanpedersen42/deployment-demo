apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "hpa-video.fullname" . }}-test-connection"
  labels:
    {{- include "hpa-video.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "hpa-video.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
